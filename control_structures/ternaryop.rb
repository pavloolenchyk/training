#!/usr/bin/ruby

numbers = [20, 49, 76, 141, 38]                       # массив чисел

numbers.each do |i|                                   # метод
 puts i.to_s + (i.even? ? " is even" : " is odd")     # якщо число парне, написати число і що воно парне - у будь-якому іншому випадку виписати число і що воно непарне
end
